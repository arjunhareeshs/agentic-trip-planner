# ========================================================
# RAG Pipeline — Python Dependencies
# ========================================================
# Install with: pip install -r requirements.txt
#
# Tested on Python 3.10+
# For CUDA support, replace `torch` with the CUDA wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ========================================================

# ── Core framework ────────────────────────────────────
llama-index>=0.10.0
llama-index-core>=0.10.0

# ── PDF parsing ───────────────────────────────────────
docling>=2.0.0
PyMuPDF>=1.23.0          # fitz — image extraction

# ── Embeddings ────────────────────────────────────────
sentence-transformers>=2.6.0    # BGE text embedding + cross-encoder
transformers>=4.38.0            # CLIP model and processor
torch>=2.1.0                    # deep learning runtime
Pillow>=10.0.0                  # image loading/processing

# ── Configuration ─────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# ── Numerical ─────────────────────────────────────────
numpy>=1.24.0

# ── Optional: accelerate inference ────────────────────
# accelerate>=0.26.0            # uncomment for faster model loading
# bitsandbytes>=0.42.0          # uncomment for 8-bit quantization (Linux only)

# ── Optional: vector store backends ───────────────────
# pinecone-client>=3.0.0        # uncomment for Pinecone
# weaviate-client>=4.0.0        # uncomment for Weaviate
# chromadb>=0.4.0               # uncomment for ChromaDB
